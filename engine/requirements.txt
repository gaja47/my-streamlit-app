# WebSocket gateway (run.py) — required to serve frames to the frontend
websockets>=12.0
# Parquet capture recorder (optional; RECORD=true)
pyarrow
# Fyers feed decode — only needed if NOT reusing opticore's live feed
protobuf
fyers-apiv3
